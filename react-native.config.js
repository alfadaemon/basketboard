module.exports = {
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  assets: ['./assets/fonts/'], // stays the same
  // commands: require(''), // formerly "plugin", returns an array of commands
};
